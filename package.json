
{
  "name": "infossewa",
  "version": "1.0.0",
  "description": "Nepal's Information Portal",
  "scripts": {
    "build": "mkdir -p dist/uploads && cp index.html index.tsx App.tsx constants.tsx types.ts metadata.json _redirects dist/ && cp -r components pages admin dist/ 2>/dev/null || true",
    "start": "echo 'Directly open index.html with a local server'"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}
